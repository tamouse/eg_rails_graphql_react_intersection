<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @post.content %>
</p>

<p>
  <strong>Published:</strong>
  <%= @post.published %>
</p>

<%= link_to_if current_user.present?, 'Edit', edit_user_post_path(user_id: current_user.id, @post) %> |
<%= link_to 'Back', user_posts_path %>
